import { RenderData } from "streamlit-component-lib";
/**
 * Returns `RenderData` received from Streamlit after the first render event received.
 */
export declare const useNullableRenderData: () => RenderData | undefined;
